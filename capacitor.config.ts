import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.dashback.app',
  appName: 'dashback',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
